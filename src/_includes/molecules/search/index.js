import './_index.scss';

class Search {
    constructor(className="m-search") {
        this.classSelector = `.${className}`;
    }

    init() {
        // Do something
        console.log('hello')
    }
}

export default Search;