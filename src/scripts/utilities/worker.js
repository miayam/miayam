/*
    import dynamically.
    after imported, display message.
    input field on the UI will trigger
    event to web worker. Web worker
    will then fetch api, do search
    and send it back to main thread.
*/
